/**
 * Detects potential PHI (Protected Health Information) in the given text.
 * @param text - The input text to scan.
 * @returns An array of detected PHI matches.
 */
export function detectPhi(text: string): string[] {
  // TODO: implement PHI detection logic
  return [];
}
